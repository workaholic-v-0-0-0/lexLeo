# legacy

Archived legacy code kept for reference during the rewrite.

This directory is not part of the active codebase and is not expected to be
fully buildable.
The active implementation lives under `src/`.
