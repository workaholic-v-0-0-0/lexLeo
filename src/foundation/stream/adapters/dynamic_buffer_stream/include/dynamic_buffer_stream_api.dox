/**
 * @defgroup dynamic_buffer_stream_api dynamic_buffer_stream API
 * @ingroup stream_family_group
 *
 * @brief Dynamic-buffer-backed adapter for the `stream` port.
 *
 * @details
 * The `dynamic_buffer_stream` adapter implements the `stream` port using an
 * in-memory dynamic buffer.
 *
 * This module belongs to the `stream` adapter family and provides the
 * functionality required to expose dynamic-buffer-backed streams through the
 * `stream` port.
 *
 * Sub-APIs:
 * - @ref dynamic_buffer_stream_cr_api
 */
