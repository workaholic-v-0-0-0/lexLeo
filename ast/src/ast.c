#include "ast/ast.h"

#include "data_structures/list.h"
#include "data_structures/tree.h"

#include <stddef.h>

char * to_string(ast ast) {
    if (ast == NULL) return NULL;
    return NULL; // placeholder
}
