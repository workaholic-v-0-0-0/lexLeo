- explain Mechanism of a non-port module for providing creators in
  docs/developer/architecture/module_taxonomy.md

- adjust `osal_file_ops_t::read()` unit tests after EOF status semantics update

  Update expected statuses for read scenarios affected by end-of-file:
  - reads requesting more elements than available;
  - reads from an empty file;
  - reads after EOF;
  - `size > 1` reads that reach EOF.

  Final convention:
  - exact read of remaining content -> `OSAL_FILE_STATUS_OK`;
  - read attempt beyond available content -> `OSAL_FILE_STATUS_EOF`.

- align `dynamic_buffer_stream_close()` specifications with updated
  `stream_close_fn_t` contract returning `stream_status_t`

- fix logger_default unit tests after stream migration

- review stream adapter argument validation policy

  Some adapter CR entry points currently validate nested creation arguments
  defensively and return `STREAM_STATUS_INVALID`, for example file path and
  mode fields in `stream_file_creator_args_t`.

  Consider migrating to the following convention:
  - keep top-level NULL checks as runtime validation;
  - treat fields of non-NULL argument structures as contract/precondition data;
  - check such invariants with `LEXLEO_ASSERT()` in development builds;
  - update specifications and tests accordingly.


- rename all *_default_env() fct by replacing the substring "_default"
  by the substring "_make" from this sufix except lexleo_app_default_env()

- logger_default has a stream_t* field in its env_t ; migrate that to align
  with new architecture

- Make `stream_create()` reject NULL backends and remove the "no backend" state from the stream port.
- Remove obsolete unit tests covering NULL backend scenarios.

- refactor module API layering and CMake architecture with authority stack
  as in stream and lexleo_vm

<here> continue migration "port-adapter decoupling" ;
       already done:
         - src/foundation/stream_new/*
         - src/foundation/logger_new/*

        WIP: <here> migrate lexleo_app with stream_new ; in lexleo_app.c
        I stopped that by desactivating lexleo_app and frontend compilation via "#add_subdirectory(lexleo_app)" and "#add_subdirectory(frontend)"
        because it depends on logger that in turn depends on stream ; hence I
        must migrate logger before. -> This is done now! -> So I go back to lexleo_app by "reactivating add_subdirectory(lexleo_app)"" ; it consists of make it depend on *_new rather than stream and logger old versions

        <here>refact lexleo_vm_t in order to avoid a dep of the kind lexleo_vm_borrower -> stream_owner due to the presence of stream creators

    Think!: What is to be opaque in handle is simultaneously "external dep" and owned
